# AGENTS.md — Єдиний протокол та Авто-Скіл для AI Асистентів
(KiloCode, Nemotron, Kimi K2.6, Cline, Black Box, Antigravity)

## ⚡ АВТОМАТИЧНИЙ ТРИГЕР: Скіл Повного Аудиту (Full Codebase Audit Skill)

**ЯКЩО КОРИСТУВАЧ ПРОСИТЬ:**
- "Зроби аудит", "Проаналізуй весь проєкт", "Огляд коду", "Проведи аудит", "Перевірь проєкт", "Full audit", "Code review" або будь-яке подібне прохання про аналіз проєкту:

**ТИ ПОВИНЕН АВТОМАТИЧНО ВИКОНАТИ НАСТУПНИЙ АЛГОРИТМ (БЕЗ УЧАСТІ КОРИСТУВАЧА):**

1. 🛑 **СУВОРЕ ТАБУ:** НЕ шукай файли через `find`/`grep` і НЕ роби паралельних запитів на читання окремих файлів. Це призводить до помилки Nvidia `ResourceExhausted (32/32)`.
2. 📄 **АВТОМАТИЧНЕ ЧИТАННЯ:** Відразу відкривай та читай єдиний файл проєкту: [repomix-output.xml](file:///d:/Chrome%20Extension/%D0%92%D1%80%D0%B5%D0%BC%D1%8F%20%D0%BF%D0%B5%D1%80%D0%B5%D0%BC%D0%B5%D0%BD.%20Chrome%20Extension/streamyars-copy-buttons%20%28added%20checkbox%29%200.6-2026.01.11/repomix-output.xml) у корені.
3. 🔄 **ЯКЩО ФАЙЛ ВІДСУТНІЙ:** Виконай у терміналі команду `npm run pack` (або `npx repomix`), щоб створити його, і потім зчитай його за один запит.
4. 📝 Проаналізуй весь код за один раз та видай результати аудиту користувачу.

---

## 🛠️ Основні команди проєкту
- `npm run dev` — запуск Vite сервера розробки
- `npm run build` — збірка розширення (Vite + CRX)
- `npm run test` — запуск тестів Node.js
- `npm run pack` — склеювання проєкту в один файл `repomix-output.xml` (контекст ~350k токенів)

---

## 📐 Правила розробки
- Chrome Extension (Manifest V3) на TypeScript / Vite.
- Перевіряй відсутність помилок збірки та тестів (`npm run test` / `npm run build`) після внесення змін.
