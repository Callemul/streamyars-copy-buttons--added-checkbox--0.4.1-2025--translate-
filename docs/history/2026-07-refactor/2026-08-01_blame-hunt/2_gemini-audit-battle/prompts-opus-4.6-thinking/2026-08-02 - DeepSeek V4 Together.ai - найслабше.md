## Як давати цей файл кодерам — аналіз

### Два варіанти:

| | 🅰️ Дати файл цілком | 🅱️ Ділити по фазах |
|:---|:---|:---|
| **Якість** | Ризик:19 задач — когнітивне перевантаження. Кодер губиться, плутає залежності. | ✅ Кожна фаза — логічно завершений шматок. Залежності вже враховані автором. |
| **Токени** | ~7KB файл × кожна сесія = марнотратство. | ✅ Фаза 1: ~2KB, Фаза 2: ~1KB, Фаза 3: ~2.5KB, Фаза 4: ~1.5KB. Економія60-80% токенів. |
| **Контроль** | Кодер сам вирішує пріоритети → може почати з Phase 4 замість Phase 1. | ✅ Ви контролюєте порядок. Спочатку критичне → потім решта. |
| **Валідація** | Важко перевірити, на якому етапі застряг. | ✅ Після кожної фази — чекпойнт: тести, ревью, реліз. |

### Рекомендація: **Ділити на фази. Однозначно.**

Автор задач вже передбачив це (див. секцію «Дорожня карта виконання» наприкінці файлу). Структура готова — просто беріть і використовуйте.

---

## Як саме ділити — практична інструкція

### 📦 Фаза 1 — 🚨 Терміново (віддаєте ПЕРШОЮ)

**Що дати кодеру:**
- Вирізати з файлу задачі **H1, H2, H3, H4, L2** + секцію «Дорожня карта» (щоб бачив контекст)
- Додати 4 файли з кодом, які він змінюватиме:
  - `youtube/studio/studio_events.ts`
  - `modules/stats_tracker.ts`
  - `modules/stats_exporter.ts`
  - `youtube/studio/studio_styles.css.temp`

**Стартовий промпт для кодера:**
> «По цих 5 задачах зроби зміни. Це критичні баги, які блокують реліз. Після виконання — запусти збірку, переконайся що все компілюється. Оцінка ~8 год роботи.»

**Чому:**5 задач, один файл (studio_events.ts) фігурує у двох задачах — даєш один раз, кодер вирішує обидві. Найвищий ROI.

---

### 📦 Фаза 2 — 🔧 Стабілізація

**Що дати:**
- Задачі **M1, M3, L6, L7** (~3 год)
- Файли: `popup_init.js`, `popup_telegram.js`, `popup_prayers.js`, `studio_content.ts`, `main.ts`

**Стартовий промпт:**
> «Фаза 1 вже зроблена. Тепер ці 4 задачі — дрібні виправлення для стабільності. M1 — найбільша, решта прості.»

---

### 📦 Фаза 3 — 🏗️ Архітектурна міграція (найбільша)

**Що дати:**
- Задачі **H5 + L5 + M2 + M4 + L9 + L10** (~12 год)
- Але **НЕ всі одразу!** Поетапно: спочатку H5+L5 (міграція на TS), потім M2 (SYH_STORAGE), потім M4 (неймспейсінг)

**Стартовий промпт (етап 3.1):**
> «Мігруй popup_init.js → popup_init.ts: забери всі window.* функції, зроби ES-модуль з import/export, підключи через script type=module у popup.html. Це перший з трьох файлів.»

Після успіху — так само для popup_telegram.js і popup_prayers.js.

---

### 📦 Фаза 4 — ✨ Шліфування

- Задачі **L1, L3, L4, L8** (~8 год)
- Можна дати всі разом — вони незалежні, ризик низький

---

## 💡 Додаткові поради для економії токенів

1. **Не давай весь оригінальний файл.** Вирізай тільки потрібну фазу + секцію залежностей (Mermaid-граф на початку).
2. **Файли коду — тільки ті, що змінюються.** Якщо задача стосується1 файлу, не треба давати весь проект.
3. **Контекст — одним абзацом.** Перед кожною фазою напиши 1-2 речення: що вже зроблено, що далі.
4. **Після кожної фази — перевірка.** Попроси кодера показати diff або результат `npm run build`. Не переходь до наступної фази, поки попередня не підтверджена.

### Разом:
- ❌ **Не давай весь файл** — 19 задач в одній сесії це хаос
- ✅ **Діли на4 фази** — автор вже це зробив за тебе
- 🔑 **Фаза 3 — додатково поділи** на 3 етапи (по одному файлу за раз)
- 📊 **Економія токенів:** ~70% порівняно з «дати все одразу»
- 🎯 **Якість:** вища, бо кодер фокусується на 3-5 задачах за раз