# Список задач: наведення порядку в проєкті (2026-09-10)

> **Джерело:** обстеження репозиторію Claude Opus 5, 2026-09-10. Не аудит коду — аудит **порядку**:
> синхронізація, гігієна репозиторію, вказівки агентам, структура тек, документація.
> **Базова лінія (гілка `v0.5.3`, застаріла):** `npm test` → 2113/2113 ✅ · `tsc --noEmit` → 0 ✅ · `lint` → 0 ✅
> **Ціль:** `origin/main` @ `b186f49`, версія 1.1.0, 18/20 задач аудиту 2026-09-08 закрито.
> **Не закрито з попереднього набору:** T10 (i18n) — рішення прийнято, варіант (б); T20 (шари в `modules/`).

---

## Мапа моделей для цього проєкту

`docs/CODEX_MODEL_SELECTION_GUIDE.md` описує правильну логіку вибору, але називає моделі Codex.
Робота йде в Claude Code, тому діє така відповідність (задача C2.5 закріплює її в довіднику):

| Роль у довіднику | Модель Claude | Коли |
|---|---|---|
| `gpt-5.6-sol` — наскрізне мислення | **Opus** | shared API, стан через кілька модулів, дані користувача, неповна інформація |
| `gpt-5.6-terra` — обмежена реалізація | **Sonnet** | рішення вже прийняте, чіткі критерії приймання, обмежений набір файлів |
| `gpt-5.6-luna` — механічна робота | **Haiku** | детерміноване перейменування, переміщення, видалення |

Правило з довідника лишається: не брати слабшу модель лише тому, що задача *називається* дрібною.

---

## Хвилі і залежності

```
Хвиля 0  синхронізація              ← без субагента, руками, з підтвердженням
   │
   ├── Хвиля 1  гігієна репозиторію    (Haiku)    ┐
   ├── Хвиля 2  вказівки агентам       (Sonnet)   ├─ паралельно, ownership не перетинається
   └── Хвиля 3  документація           (Sonnet)   ┘  (C3 стартує після C1+C2 — зводить посилання)
                                   │
                       ⏸ РУЧНА ПЕРЕВІРКА 1.1.0 в браузері
                                   │
       Хвиля 4  код: T20, дрібні файли, фасади, селектори, i18n
```

**Максимум 3 паралельні агенти** з неперетинним file ownership — правило `AGENTS.md`.
Кожна задача завершується `npm run verify` і **окремим комітом**.

---

## Хвиля 0 — синхронізація (без субагента)

| # | Задача | Що зробити | Як перевірити |
|---|---|---|---|
| **C0.1** | Робоча копія на 23 коміти позаду | Перейти на `origin/main`. `v0.5.3` — предок `origin/main`, тому нічого не втрачається | `git merge-base --is-ancestor v0.5.3 origin/main` → 0; `npm run verify` зелений на новому дереві |
| **C0.2** | Локальний `main` має один сторонній коміт `abfc840` «помилка» (08.07.2025, код до TypeScript) | Вказати локальний `main` на `origin/main`, коміт відкинути | `git log main..origin/main` порожній |
| **C0.3** | Мертві гілки | Видалити `review/t7` (повністю влита), `subagent-Code-Bugfixer-…`, `subagent-UI-UX-Designer-…` | `git branch --merged origin/main` не містить зайвого |
| **C0.4** | Живий worktree `.kilo/worktrees/manual-testing` з `.js`-кодом від 12.2025 | `git worktree remove`, гілку `manual-testing` — за рішенням власника | `git worktree list` → лише корінь |

---

## Хвиля 1 — гігієна репозиторію · **Haiku**

**Ownership:** корінь репозиторію, `.gitignore`, `.idea/`, `RTK.md`, `.clineignore`, `.kilocodeignore`, `.agents/mcp_config.json`

| # | Задача | Що зробити | Як перевірити |
|---|---|---|---|
| **C1.1** | `.idea/` відстежується git'ом попри `.gitignore`; всередині `page-redder.iml` — назва з іншого проєкту | `git rm -r --cached .idea` | `git ls-files .idea` порожній, файли на диску лишились |
| **C1.2** | `RTK.md` у корені репозиторію — опис локального CLI власника, дублює `~/.claude/RTK.md` | Прибрати з репозиторію (файл на диску лишити) | `git ls-files RTK.md` порожній; жоден трекнутий файл на нього не посилається (див. C2.2) |
| **C1.3** | Конфіги трьох асистентів, якими проєкт більше не ведеться | Видалити `.clineignore`, `.kilocodeignore`, `.agents/mcp_config.json` (оголошує лише `fallow-mcp`) | `npm run verify` зелений; у корені немає `*ignore` без споживача |
| **C1.4** | ~96 МБ мертвих кешів у робочій теці: `.kilo/` 60 МБ (з власними `node_modules`), `.codesearch.db/` 31 МБ, `.fallow/` 4.5 МБ, `coverage/` 1.4 МБ | **Спершу підтвердження власника на видалення з диска.** Усі вже в `.gitignore` | Тека проєкту меншає на ~96 МБ; `git status` чистий |
| **C1.5** | `walkthrough.md` у корені, хоча `docs/README.md` сам позначає його «історичний» | `git mv walkthrough.md docs/history/walkthrough.md` | Посилання в `docs/README.md` живе (звести в C3.4) |

---

## Хвиля 2 — вказівки агентам · **Sonnet**

**Ownership:** `AGENTS.md`, новий `CLAUDE.md`, `.agents/skills/*`, `docs/CODEX_MODEL_SELECTION_GUIDE.md`, `docs/dom_snapshots/`

| # | Задача | Що зробити | Як перевірити |
|---|---|---|---|
| **C2.1** | Немає `CLAUDE.md` — Claude Code не гарантує читання `AGENTS.md` | Створити `CLAUDE.md` на 3–5 рядків: посилання на `AGENTS.md` як єдине джерело. **Правил не дублювати** | Файл існує, містить лише вказівник |
| **C2.2** | `AGENTS.md` наказує користуватись MCP-інструментами, яких не існує: §3 «віддавай перевагу `codebase-memory`», §8 — 15 рядків схем `search_code` / `get_symbols_tool` / `get_lines_tool`; у конфізі оголошено лише `fallow`. Плюс §3 забороняє «view_file для файлів > 50 рядків» | Видалити §8 повністю. З §3 прибрати `codebase-memory` і заборону читання файлів. Прибрати `@RTK.md` з першого рядка | У `AGENTS.md` не згадується жоден неоголошений інструмент; файл читається без зовнішніх імпортів |
| **C2.3** | Скіли порушують власне правило T14: `.agents/skills/streamyard.md` містить 5 захардкоджених селекторів (`PlatformComment__Wrap`, `Banner__LiWrap`, `StarredCommentList__List`) | Замінити на посилання виду «селектор → `modules/config.ts`, `SYH_CONFIG.SELECTORS.commentBlock`». Те саме перевірити для `youtube-studio.md` (`youtube/studio/studio_selectors.ts`) | `git grep 'class\*=' .agents/skills/` → 0 збігів |
| **C2.4** | `docs/dom_snapshots/README.md` оголошує «золоте правило: звірся з файлами в цій папці», а снепшотів там **нуль** | Переписати правило в здійсненне: «якщо снепшот є — звірся; якщо нема — зніми з DevTools і поклади сюди». Три обіцяні файли (`afk_dialog.html`, `comments.html`, `banners.html`) або зняти зі списку, або позначити як TODO | Правило не посилається на неіснуючі файли |
| **C2.5** | Довідник моделей називає моделі Codex (`gpt-5.6-sol/terra/luna`), робота йде в Claude Code | Перейменувати на `docs/MODEL_SELECTION_GUIDE.md`, додати мапу Opus / Sonnet / Haiku (розділ «Мапа моделей» цього файла). Логіку вибору лишити 1-в-1 — вона правильна | Посилання з `AGENTS.md` §7 живе; жодна модель Codex не згадується як робоча |

---

## Хвиля 3 — документація · **Sonnet** (після C1 і C2)

**Ownership:** `docs/` крім `docs/rules/`, `README.md`, `docs/README.md`

| # | Задача | Що зробити | Як перевірити |
|---|---|---|---|
| **C3.1** | `docs/рефактор/` — 48 файлів / 548 КБ зі шляхами до ~200 символів: кирилиця + пробіли (`.../2 - аудит Gemini різними - батл/prompts-for-coders - from OPUS 4.6 Thinking/...`). На Windows це ризик довжини шляху і повний блок для пошуку | `git mv` у `docs/history/2026-07-refactor/` з ASCII-іменами тек. **Вміст файлів не чіпати** — це історія | Діф містить лише перейменування; `git log --follow` на вибірковому файлі показує стару історію |
| **C3.2** | Архів аудитів порушує власну конвенцію `YYYY-MM-DD_<MODEL>_AUDIT.md`: безіменні `audits/archive/AUDIT.md` і `TASKS.md`, дубль `…_KiloCode_Laguna_S_2.1_AUDIT 2.md` | Перейменувати за конвенцією (дату брати з `git log` файла). Дубль звірити і лишити один | У `docs/audits/` немає файлів без дати й моделі в імені |
| **C3.3** | `docs/manual testing/resources/test-Popup-Telegram.md` — 0 байт, поруч із повною копією на рівень вище | Видалити порожній файл і порожню теку `resources/` | Файл відсутній, посилань на нього немає |
| **C3.4** | Після C1.5, C2.5, C3.1–C3.3 частина посилань у навігації стане мертвою | Звести `README.md` і `docs/README.md`: усі шляхи існують, розділ «Документи в корені» оновлений (без `RTK.md`, без `walkthrough.md`) | Скриптом перевірити кожне посилання в обох файлах → 0 битих |
| **C3.5** | `docs/lock-cells/` — 772 рядки плану на нереалізовану роботу, доданий у історію **двічі** (`7d7d524` і `b186f49`, ідентичний вміст) | Позначити статус у `docs/README.md`: план, не реалізовано, дата. Або перенести в `docs/plans/` до решти планів | `docs/README.md` називає статус цієї теки |
| **C3.6** | `docs/audits/active/` тримає 3 файли за закриті задачі | **Після ручної перевірки 1.1.0** перенести в `docs/audits/archive/2026-09-10_v1.1/`. Цей файл лишається в `active/` | В `active/` лише незакриті набори |

---

## ⏸ Ручна перевірка 1.1.0 — умова для хвилі 4

`Release_notes.md` прямо каже, що перевірка в браузері не проводилась; чекліст —
`docs/manual testing/2026-09-09_NIGHT_CHECKLIST.md`. Автотести на happy-dom не бачать
ні реального StreamYard, ні реального `chrome.storage`.

**Чому це блокує хвилю 4:** C4.3b (T20) змінює шляхи імпортів у всіх 286 файлах. Якщо запустити
його до перевірки — знайдений баг буде неможливо однозначно віднести до 1.1.0 або до переміщення.

---

## Хвиля 4 — код (після ручної перевірки)

| # | Задача | Модель | Ownership | Що зробити | Як перевірити |
|---|---|---|---|---|---|
| **C4.1** | Мертві barrel-фасади: `modules/event_banners.ts` (1 споживач), `modules/comment_assistant.ts` (4), `modules/streamyard_comments.ts` (1) — файли лише з `export *` «для сумісності», яку вже нема кому ламати | **Haiku** | 3 файли + 6 місць імпорту | Замінити імпорти на прямі шляхи, фасади видалити | `npm run verify`; `git grep "export \*" modules/*.ts` → 0 |
| **C4.2** | 5 захардкоджених селекторів у продакшн-коді поза реєстром `config.ts`: `bootstrap_messages.ts`, `comment_assistant/trigger_words.ts`, `event_banners/mouseup_handler.ts`, `stats_auto_phase.ts`, `streamyard_comments/auto_heal.ts` | **Sonnet** | ці 5 файлів + `modules/config.ts` | Внести в `SYH_CONFIG.SELECTORS`, споживачів перевести на реєстр. Врахувати, що в `auto_heal.ts` це **fallback** — семантику зберегти | `git grep 'class\*=' -- 'modules/*.ts'` → лише `config.ts`; `npm run verify` |
| **C4.3a** | **T20, проєктування.** 173 файли пласко в `modules/`, тоді як `ARCHITECTURE.md` §2 описує 4 шари. Документ і дерево тек не збігаються | **Opus** | лише новий документ | Скласти таблицю «звідки → куди» для всіх 173 файлів за шарами з `ARCHITECTURE.md` (Presentation / Orchestration / Domain / Infrastructure) або за доменами (`comments/`, `banners/`, `stats/`, `telegram/`, `storage/`, `ui/`). **Коду не чіпати.** Обґрунтувати вибір осі поділу | Кожен файл згаданий рівно один раз; жоден цикл імпортів не з'являється |
| **C4.3b** | **T20, виконання** | **Sonnet** | `modules/`, `youtube/`, `popup/`, `options/`, `main.ts`, `tests/` | Виконати переміщення за таблицею C4.3a одним комітом: тільки `git mv` + правка шляхів імпортів | Діф містить **лише** переміщення і шляхи; `npm run verify` зелений |
| **C4.4** | Надмірне дроблення: 108 із 286 `.ts` мають < 60 рядків, 44 — менше 30. Хвилі рефакторингу порізали занадто дрібно | **Sonnet** | за списком, узгодженим після C4.3b | Злити файли за критерієм: **той самий домен І єдиний споживач І разом < 150 рядків**. Файли з ≥ 2 споживачами не чіпати | `npm run verify`; кількість файлів < 30 рядків падає; жоден публічний експорт не зник |
| **C4.5a** | **T10, інвентаризація.** i18n напівживий: 13 ключів × 3 локалі, 11 викликів `getMessage`, при цьому ~599 кириличних літералів у 227 `.ts`-файлах і 106 рядків у `popup.html` / `options.html` | **Sonnet** | новий документ | Розділити 599 літералів на: (1) видимі користувачу рядки UI, (2) назви категорій/даних, (3) коментарі й логи. Порахувати реальний обсяг (1). Запропонувати порядок зон | Інвентар покриває всі 227 файлів; для кожної зони названо кількість рядків |
| **C4.5b** | **T10, правило** | **Opus** | `docs/rules/i18n.md`, `AGENTS.md` §5 | Зафіксувати правило «новий видимий рядок = ключ у `messages.json`». Описати іменування ключів, роботу з `popup.html` / `options.html`, поведінку при відсутньому перекладі | Правило додано окремим файлом у `docs/rules/` і рядком у таблиці `AGENTS.md` §5 |
| **C4.5c** | **T10, винос** | **Sonnet**, хвилями по зонах | по одній зоні за раз | Виносити літерали в `messages.json` **тільки** за списком зон з C4.5a, по одній зоні на коміт. Починати з `popup.html` і `options.html` — там рядки суто користувацькі | Після кожної зони: `npm run verify` + візуальна звірка попапу |

> ⚠️ **Оцінка C4.5:** «добудувати i18n» у повному обсязі — це не одна задача. 599 літералів у 227 файлах,
> з них частина взагалі не належить UI. Тому винос розбито на підпункти хвилями, а не одним заходом,
> і **правило (C4.5b) важливіше за винос (C4.5c)**: воно зупиняє ріст боргу негайно, тоді як винос можна вести
> у фоні скільки завгодно довго. Виносити все одразу — означає зачепити ті самі файли, що й C4.3b, і зіпсувати обидва діфи.

---

## Стан виконання (оновлено 2026-09-13)

> **Базова лінія на `cf10bba`:** `npm run verify` → typecheck 0 · lint 0 · `check:architecture` без
> циклів (167 файлів, 18 доменів) · тести **2211/2211** · build ok.
> **Не запушено:** локальний `main` на **22 коміти** попереду `origin/main`.

| Хвиля / задача | Статус | Коміти |
|---|---|---|
| 0 — синхронізація | ✅ | робоча копія на `origin/main` 1.1.0; знято worktree і три мертві гілки |
| 1 — гігієна репозиторію | ✅ C1.1, C1.2, C1.3, C1.5 · ⏸ C1.4 | `7e307bd`, `50770b1`, `7f5de64` |
| 2 — вказівки агентам | ✅ C2.1–C2.5 | `c9ea19b` |
| 3 — документація | ✅ C3.1–C3.5 · ⏸ C3.6 | `6a081b9`, `f0090d1` |
| C4.1 — мертві barrel-фасади | ✅ | `edf27b2`, `14a1038` |
| C4.2 — 5 селекторів у реєстр | ✅ | `4180f45` |
| C4.3a — карта розкладки `modules/` | ✅ | `3d6c03c` → `docs/plans/T20-modules-layout.md` |
| C4.3b — переїзд T20, 4 хвилі + доки | ✅ | `b8a4bf7`, `fc33b32`, `e1bcabd`, `473f5e1`, `b826edd`, `5a48ef9` |
| C4.4 — злиття дрібних файлів | ✅ **закрито за доказами** | `63ac522` → `docs/plans/module-consolidation.md` |
| C4.5a/b — інвентар і правило i18n | ✅ | `cf10bba` → `docs/plans/i18n-inventory.md`, `docs/rules/i18n.md` |
| C4.5c — винос рядків i18n | 🟡 P1a, P1b, частина P1c | `cf10bba` |
| C4.6 — решта селекторів у реєстр | ✅ | `135a9c1` |

**Про C4.4.** Початкова гіпотеза «108 файлів < 60 рядків — надмірне дроблення» не підтвердилась.
Оцінка за споживачами й відповідальністю показала: більшість коротких файлів — осмислені межі
(окремий кеш regex, окремий DOM-контракт, окремий ефект). Виправдане злиття знайшлось лише для
трьох пар «вхід → єдина реалізація» — усі три виконано. **Масове злиття за кількістю рядків не робити**:
`module-consolidation.md` §«Keep the reviewed boundaries» перелічує, що і чому лишати.

**Позапланово знайдено й виправлено:**

- `9423c1c` — `tests/storage_schema.test.js` збирав шляхи через `join()`, тому на Windows
  сторож проти `Record<string, any>` **проходив вхолосту**.
- `14a1038` — після C4.1 два імпорти в `youtube/studio/` трималися на видаленому фасаді лише
  завдяки `moduleResolution: bundler`, який мовчки підставляв теку замість файла.
- `cf10bba` — i18n-розмітку позначили 140 ключами, але **жодного не внесли в `messages.json`**,
  попап **ніде не викликав `localize()`**, а правило не було в `AGENTS.md` §5. Доведено до ладу.
  Мок `chrome.i18n.getMessage` у `tests/setup/chrome_mock.ts` повертав назву ключа — справжній
  Chrome так не робить; виправлено. Додано сторож `tests/i18n_catalog_completeness.test.js`
  (перевірено мутацією: без ключа падає з назвою ключа і файлом).

---

## Що лишилось — на потім

Упорядковано за тим, що блокує що. Кожен пункт самодостатній: можна брати в окремому чаті.

### 🔴 1. Ручна перевірка в браузері — не проводилась жодного разу

Усе, що зроблено з 1.1.0 (T7–T19), T20 (переїзд 160 файлів), C4.4, C4.6 і i18n, перевірено
**лише автотестами на happy-dom**. Вони не бачать ні реального StreamYard, ні реального `chrome.storage`,
ні реального `chrome.i18n`.

- Чекліст: `docs/manual testing/2026-09-09_NIGHT_CHECKLIST.md` (поведінковий, шляхів до файлів не містить — T20 його не зачепив).
- **Додатково до чекліста перевірити:**
  - зібрати `npm run build`, завантажити `dist/` у `chrome://extensions`;
  - StreamYard: кнопки коментарів (ЛКМ / коліщатко / ПКМ на 🙏), банери, anti-AFK, статистика —
    це зони, де C4.6 замінив селектори;
  - попап і сторінка налаштувань у **трьох мовах браузера** (uk / en / ru) — i18n вперше змінює
    видимий текст для en/ru;
  - YouTube і YouTube Studio: панелі кнопок і бейдж.
- Від результату залежать пункти 2 і 3.

### 🟠 2. Пуш у `origin`

`main` на 22 коміти попереду `origin/main`. Робити **після** пункту 1, щоб на remote не поїхало
неперевірене. Архів у власника є.

### 🟠 3. Архівувати аудити (C3.6)

Після пункту 1: `docs/audits/active/2026-09-05_OPUS_AUDIT.md`, `2026-09-08_CLAUDE_OPUS_5_AUDIT.md`,
`2026-09-08_CLAUDE_OPUS_5_TASKS.md` → `docs/audits/archive/2026-09-XX_v1.1/`. Цей файл лишити в `active/`,
доки не закриті пункти нижче. Оновити посилання «Поточний» у `docs/README.md`.

### 🟡 4. i18n — решта зон (C4.5c)

Повний план зон і файлів — `docs/plans/i18n-inventory.md` §3 і §5. Стан:

| Зона | Стан |
|---|---|
| P0 — хелпер `localize` | ✅ |
| P1a — popup HTML | ✅ |
| P1b — options HTML + runtime | ✅ |
| P1c — popup runtime | 🟡 зроблено: порожній список молитов, попередження кімнати, шапка таблиці чистки. **Не зроблено:** `prayer_toolbar_actions.ts`, `prayer_data.ts`, `popup_telegram_counters.ts`, `popup_telegram_collected.ts`, confirm/toast/тимчасові підписи |
| **P0b — вибір мови за опцією `ui_locale`** | ❌ **Важливо:** зараз мову визначає `chrome.i18n`, тобто **мова браузера**, а перемикач мови на сторінці налаштувань **нічого не робить**. Контракт описаний у `docs/rules/i18n.md` §4–5 |
| P2 — спільні дії / StreamYard | ❌ |
| P2b — діагностика чистки Telegram | ❌ |
| P3 — YouTube / Studio | ❌ |
| P4 — банерна модалка | ❌ |
| P5 — аналітика | ❌ |
| P6 — відео | ❌ |

Правило для кожної зони: ключ у всіх трьох `_locales/*/messages.json` в тому ж коміті —
тепер це тримає сторож `i18n_catalog_completeness`.

**Дрібне до i18n:**
- в українському каталозі лишились англійські рядки, які такими були і в UI:
  `popup_transliteration` («Transliteration»), `popup_input_text` («Input text...»),
  `popup_translate_label` («Translate») — вирішити, чи перекладати;
- переклади en/ru для 138 ключів згенеровано машинно-асистовано (`cf10bba`) — **проглянути носієм мови**.

### 🟡 5. Два різні селектори зірки StreamYard

У `SYH_CONFIG.SELECTORS` (`modules/registry/config.ts`) живуть обидва:
`starButton` → `PlatformComment__StarButton` і `commentShellStarButton` → `PlatformCommentShell__StarButton`.
C4.6 навмисно не злив їх — це різні вузли DOM. Відкрите питання: **чи обидва ще існують на живому
StreamYard, чи один мертвий.** Перевіряти через DevTools за `AGENTS.md` §1, не наосліп.

### 🟡 6. Плаваючий тест

Один прогін `npm run verify` 2026-09-10 дав 2201/2202 під навантаженням (працювали два субагенти).
Наступні 7+ прогонів чисті, назва тесту не зловлена. Ймовірно таймінг-залежний. Якщо повториться —
зберегти повний вивід (`npm test > out.txt`) і шукати `✖ failing tests`.

### ⚪ 7. Дрібниці, які нічого не ламають

- **C1.4** — ~96 МБ мертвих кешів на диску поза git: `.kilo/` 60 МБ, `.codesearch.db/` 31 МБ,
  `.fallow/` 4.5 МБ, `coverage/` 1.4 МБ. Перед видаленням `.kilo/` зберегти
  `.kilo/plans/1785816614796-agent-workflow-audit-plan.md` — єдиний не-кеш файл там.
- **Гілки:** локальні `v0.5.3` і `manual-testing` повністю старі (є на remote). Плюс `stash@{0}`
  на `v0.5.3` зі старим WIP «Step 3. Hy3 High (KiloCode)» — переглянути і прибрати.
- **`tests/css_lint.test.js`** не перевіряє `modules/streamyard/modal_styles.css` — у списку лише 5 CSS-файлів.
- **Зворотні `import type` між теками** (`storage/storage_keys.ts` → `registry/`, `comments/`, `stats/`, `core/`).
  Циклу в рантаймі немає, `check:architecture` їх свідомо пропускає. Розчепити можна, винісши
  `StorageSchema` з `storage_keys.ts` — опис у `docs/plans/T20-modules-layout.md` §5. Необов'язково.

---

## Порядок запуску субагентів (історичний, як виконувалось)

| Черга | Агенти | Результат |
|---|---|---|
| 1 | C1 (Haiku) ‖ C2 (Sonnet) | ✅ |
| 2 | C3 (Sonnet) | ✅ |
| 3 | C4.1 (Haiku) ‖ C4.2 (Sonnet) | ✅; C4.1 пропустив 2 імпорти — доправлено вручну |
| 4 | C4.3a (Opus) | ✅ |
| 5 | C4.3b (Sonnet) | ✅ 4 хвилі; агент впав на ліміті API на документації — доведено окремо |
| 6 | C4.4, C4.5a/b, C4.6 | ✅ виконано в іншій сесії, лишено незакоміченим; закомічено й доведено 2026-09-13 |

**Уроки для наступних запусків:**
- Звіт субагента — не доказ. Щоразу перевіряти грепом і `npm run verify` самому: в цій сесії двічі
  знаходились пропуски, які звіт описував як виконане.
- Паралельні агенти в одному дереві не мають запускати `build`/`verify` одночасно — конфлікт на `dist/`.
- Сторожові тести, що читають файли за шляхом, перевіряти мутацією — вони схильні мовчки проходити вхолосту.
