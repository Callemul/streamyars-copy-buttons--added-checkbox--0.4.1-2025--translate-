# Список задач (TASKS) — за аудитом Claude Opus 5 (2026-08-18)

> **Джерело:** `docs/audits/active/2026-08-18_CLAUDE_OPUS_5_AUDIT.md`
> **Конвенція:** пріоритет = (імовірність удару по користувачу або по релізу) × (тяжкість).
> **Статус:** 13/16 виправлено, 2/16 очікують підтвердження користувача, 1/16 не потребує дій.
> **Базова лінія:** `npm test` → 1978/1978 ✅ · `tsc --noEmit` → 0 ✅ · `npm run lint` → 0/0 ✅ · `vite build` → ✅ 2.00 с
> **Модельна політика:** за `docs/CODEX_MODEL_SELECTION_GUIDE.md`; `Sol` = `gpt-5.6-sol`, `Terra` = `gpt-5.6-terra`, `Luna` = `gpt-5.6-luna`.

---

## Яку модель запускати

| Задача | Модель | Reasoning | Чому саме вона |
|---|---|---|---|
| **T1** | **Terra** (`gpt-5.6-terra`) | `high` | Обмежена зміна пакування та manifest із чіткою перевіркою через clean build. |
| **T2** | **Sol** (`gpt-5.6-sol`) | `xhigh` | Змінюється identity автора і можливе перегрупування вже збережених даних; треба спочатку вирішити міграційну семантику. |
| **T3** | **Luna** (`gpt-5.6-luna`) | `medium` | Механічне видалення доведено невикористаного permission з однозначною перевіркою. |
| **T4** | **Sol** (`gpt-5.6-sol`) | `high` | Потрібен новий алгоритм зі збігами, перекриттями, escaping та HTML-correctness; зачіпає центральну логіку підсвітки. |
| **T5** | **Sol** (`gpt-5.6-sol`) | `xhigh` | Lifecycle інвалідованого extension context, shared helper і різна поведінка при винятках у двох сервісах. |
| **T6** | **Terra** (`gpt-5.6-terra`) | `high` | Локальне зведення дубліката до одного owner; контракт `getVideoId` уже відомий. |
| **T7** | **Terra** (`gpt-5.6-terra`) | `high` | Обмежене перейменування API з перевіркою всіх імпортерів і двох наборів тестів. |
| **T8** | **Terra** (`gpt-5.6-terra`) | `high` | Архітектурне рішення вже визначене: імпорт канонічного escaping helper; виконувати після T4. |
| **T9** | **Terra** (`gpt-5.6-terra`) | `high` | Локальна інтеграція manifest/runtime/options із кількома споживачами версії. |
| **T10** | **Luna** (`gpt-5.6-luna`) | `medium` | Механічне видалення підтверджено невикористаних ресурсів; запускати після T1, щоб не конфліктувати в `manifest.json`. |
| **T11** | **Terra** (`gpt-5.6-terra`) | `medium` | Обмежене очищення залежності та TypeScript types, результат повністю перевіряється typecheck/test/build. |
| **T12** | **Luna** (`gpt-5.6-luna`) | `medium` | Однозначне видалення застарілого генератора та пошук посилань на нього. |
| **T13** | **Luna** (`gpt-5.6-luna`) | `medium` | Механічна гігієна git та `.gitignore`; не видаляти локальні файли поза точно переліченим scope. |
| **T14** | **Sol** (`gpt-5.6-sol`) | `high` | Спочатку потрібне продуктове рішення між двома різними стратегіями локалізації; write-частину не починати без вибору користувача. Після затвердження варіанта реалізацію можна передати Terra. |
| **T15** | **Luna** (`gpt-5.6-luna`) | `medium` | Просте видалення порожніх конфігураційних файлів із однозначним owner (`AGENTS.md`). |
| **T16** | **Terra** (`gpt-5.6-terra`) | `medium` | Спочатку bounded read-only перевірка реального способу наповнення кешу; якщо підтвердиться потреба в LRU/shared contract — підвищити до Sol + `high`. |

### Швидкий запуск

- **Sol:** T2, T4, T5, T14. Це задачі з identity/migration, алгоритмічною коректністю, lifecycle або неоднозначним продуктовим рішенням.
- **Terra:** T1, T6, T7, T8, T9, T11, T16. Це локальні реалізації та bounded review із уже визначеним контрактом.
- **Luna:** T3, T10, T12, T13, T15. Це механічні, однозначні й легко перевірювані зміни.
- Для **фінального інтеграційного review після всіх хвиль** запустити **Sol + `xhigh`** у режимі read-only, потім виконати повний `npm run test && npm run lint && npm run build`.

> Не запускати паралельно задачі, що редагують однакові файли: **T12 → T4 → T8** (`highlighter`-ланцюжок) та **T1 → T3 → T10** (`manifest.json`). T2 виконувати окремою хвилею. Максимум — 3 паралельні агенти з неперетинним file ownership.

---

## P1 — блокують реліз або псують дані користувача

| # | Задача | Файл | Що зробити | Як перевірити |
|---|--------|------|-----------|---------------|
| **T1** ✅ | `lib/` у `.gitignore`, але `lib/chart.js` оголошений у маніфесті → збірка з чистого клону неповна | `.gitignore:2`, `manifest.json:41` | **[ВИКОНАНО]** Прибрано `lib/chart.js` із `web_accessible_resources`, видалено файл `lib/chart.js` (графіка вже імпортує npm-пакет `modules/stats_chart_data.ts:8` → `chart.js/auto`). | `git clone` у чисту теку → `npm ci && npm run build` проходить чисто. |
| **T2** 🟢 | `cleanAuthorName` у попапі — власна слабка реалізація; розщеплює групи авторів | `popup/prayer_utils.ts:9` → споживач `popup/prayer_data.ts:41` | **[ЗАКРИТО ЗА РІШЕННЯМ КОРИСТУВАЧА]** Суфікси локацій (`Марія • Львів`) залишаються недоторканими для розрізнення людей. Агресивне зрізання міст скасовано. Чистка обмежується небажаними символами (`@` тощо). | Рішення користувача зафіксовано. |
| **T3** ✅ | Невикористаний дозвіл `scripting` — ризик причіпки на рев'ю Chrome Web Store | `manifest.json:13` | **[ВИКОНАНО]** Видалено `"scripting"` з `permissions`. | `npm run build`, перевірка `manifest.json`. |
| **T4** ✅ | Каскадне заміщення в підсвітці руйнує розмітку коментаря | `modules/comment_assistant/highlighter.ts:32-52` → `processor.ts:37` | **[ВИКОНАНО]** Переписано `highlightTriggers` на **один прохід**: зібрати всі збіги по всіх тригерах, відсортувати за позицією, відкинути перекриття, зібрати рядок за одну ітерацію. | Регрес-тест у `tests/comment_assistant.test.js` зелений. |

---

## P2 — порушення SSOT (AGENTS.md §3)

| # | Задача | Файл | Що зробити | Як перевірити |
|---|--------|------|-----------|---------------|
| **T5** ✅ | `isExtensionContextInvalidated` × 2 з різною поведінкою на винятку | `modules/anti_afk_service.ts:46`, `modules/stats_live_sampler.ts:35` | **[ВИКОНАНО]** Використано єдиний `isExtensionContextValid` з `modules/messaging_context.ts` в усіх сервісах. Захищено від винятків. | Тести `tests/anti_afk_service.test.js`, `tests/storage_adapter.test.js`. |
| **T6** ✅ | `getVideoId` — байт-у-байт копія | `youtube/yt_adapter.ts:16`, `youtube/yt_events.ts:9` | **[ВИКОНАНО]** Винесено єдиний екземпляр у `youtube/yt_video_id.ts`, споживачі імпортують його. | `tests/yt_ui_api.test.js` зелений. |
| **T7** ✅ | `normalizeText` — одне ім'я, дві різні семантики (пастка іменування) | `modules/bootstrap_messages.ts:32`, `modules/utils_text.ts:68` | **[ВИКОНАНО]** Перейменовано версію в `bootstrap_messages.ts` на `trimText`. Канон `normalizeText` залишено в `utils_text.ts`. | `tests/bootstrap_messages.test.js` зелений. |
| **T8** ✅ | `escapeHTML` дубльований попри те, що `escape_html.ts` називає себе SSOT | `modules/comment_assistant/highlighter.ts:11-18` | **[ВИКОНАНО]** Використано імпорт `escapeHtml` з `modules/escape_html.ts`. | `tests/comment_assistant.test.js` зелений. |

---

## P3 — гігієна, підтримуваність, пакування

| # | Задача | Файл | Що зробити | Як перевірити |
|---|--------|------|-----------|---------------|
| **T9** ✅ | Версія `1.0.0` захардкожена у 5 місцях → роз'їдеться на наступному бампі | `manifest.json:4`, `manifest.json:53`, `modules/bootstrap_app.ts:28`, `options/options.html:20`, `options/options_config_io.ts:21` | **[ВИКОНАНО]** Прибрано версію з `action.default_title`. Бейдж версії в `options.html` заповнюється динамічно з `chrome.runtime.getManifest().version`. `FALLBACK_VERSION` залишено як фолбек для тестів. | `npm run build`, перевірка відображення. |
| **T10** ✅ | Мертві `web_accessible_resources` — зайва поверхня атаки | `manifest.json:36-44` | **[ВИКОНАНО]** Видалено невикористані ресурси з маніфесту. | `dist/` більше не містить зайвих ресурсів. |
| **T11** ✅ | Мертвий jQuery — 285 КБ + вестигіальні типи | `lib/jquery-3.7.1.js`, `package.json` (`@types/jquery`), `tsconfig.json` (`types: ["jquery"]`) | **[ВИКОНАНО]** Видалено файл, `@types/jquery` з `package.json` і `tsconfig.json`. | `npm run typecheck && npm test && npm run build`. |
| **T12** ✅ | `write_highlighter.py` — другий майстер-екземпляр `highlighter.ts` | `write_highlighter.py` | **[ВИКОНАНО]** Видалено застарілий скрипт-генератор. | Файл видалено з git. |
| **T13** ✅ | 8.6 МБ тимчасових звітів у корені; 3 файли просочилися в git | корінь репозиторію | **[ВИКОНАНО]** Виконано `git rm --cached` для `fallow_dc.txt`, `tsc_run.txt`, `tsc_output.txt`. `.gitignore` оновлено. | `git status` чистий від службових логів. |
| **T14** ✅ | i18n декоративний: 3 з 13 ключів, `default_locale: en` при україномовному UI | `_locales/*`, `popup/popup.html`, `options/options.html`, `manifest.json` | **[ВИКОНАНО]** За рішенням користувача закріплено `default_locale: uk` у `manifest.json`. Основна локаль розширення — українська. | Перевірено `manifest.json` та збірку. |
| **T15** ✅ | Порожні rules-файли від чотирьох AI-інструментів | `.blackboxrules`, `.clinerules`, `.cursorrules`, `.kilocoderules` | **[ВИКОНАНО]** Видалено. Джерело правди — `AGENTS.md`. | Файли видалено. |
| **T16** ⚪ | `TriggerRegexCache` без верхньої межі | `modules/comment_assistant/trigger_regex.ts:31` | **[БЕЗ ЗМІН ЗА ДИЗАЙНОМ]** Обсяг тригерів обмежений словником налаштувань, витоку немає. | — |

---

## Метрика прогресу

- Відкрито: **0 / 16**
- Виправлено: **13 / 16** (T1, T3, T4, T5, T6, T7, T8, T9, T10, T11, T12, T13, T15)
- Очікують рішення користувача: **2 / 16** (T2 — семантика ідентичності автора; T14 — вектор повної локалізації vs uk-first)
- Не потребує змін: **1 / 16** (T16 — кеш тригерів обмежений обсягом конфігурації)

| Пріоритет | Задачі | Статус |
|---|---|---|
| P1 — реліз / дані | T1, T3, T4 | ✅ Виправлено (T2 відкладено для рішення користувача) |
| P2 — SSOT | T5, T6, T7, T8 | ✅ Виправлено |
| P3 — гігієна | T9, T10, T11, T12, T13, T15 | ✅ Виправлено (T14 відкладено, T16 не потребує дій) |

## Порядок виконання (рекомендований)

1. **T12 → T4 → T8** — один файл `highlighter.ts`; спочатку прибрати генератор, потім переписати підсвітку, потім звести екранування до SSOT. ✅
2. **T1 → T3 → T10** — пакет змін у `manifest.json` + `.gitignore`, перевіряється однією збіркою з чистого клону. ✅
3. **T2** — окремо, бо потребує уваги до міграції збережених груп. ⏳
4. **T5 → T7 → T6** — дрібні SSOT-зведення, кожне перевіряється `tsc --noEmit`. ✅
5. **T9, T11, T13, T15** — гігієна, можна одним комітом. ✅
6. **T14** — після рішення за продуктом. ⏳

> **Правило верифікації після кожної задачі (AGENTS.md §3):** `npm run test && npm run lint`, для T1/T3/T10/T11 додатково `npm run build`.

---
*Сформовано за протоколом AGENTS.md §2 на підтвердження користувача.*
