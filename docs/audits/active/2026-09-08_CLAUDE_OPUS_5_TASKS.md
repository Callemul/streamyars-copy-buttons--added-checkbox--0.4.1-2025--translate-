# Список задач (TASKS) — за аудитом Claude Opus 5 (2026-09-08)

> **Джерело:** `docs/audits/active/2026-09-08_CLAUDE_OPUS_5_AUDIT.md`
> **Конвенція:** пріоритет = (як часто це б'є по роботі) × (скільки місць доводиться правити руками).
> **Статус:** 0/20 виконано.
> **Базова лінія:** `npm test` → 2098/2098 ✅ · `tsc --noEmit` → 0 ✅ · `npm run lint` → 0/0 ✅ · `vite build` → ✅ 4.18 с · гілка `v0.5.3` @ `def2c69`
> **Модельна політика:** за `docs/CODEX_MODEL_SELECTION_GUIDE.md`; `Sol` = `gpt-5.6-sol`, `Terra` = `gpt-5.6-terra`, `Luna` = `gpt-5.6-luna`.

---

## Яку модель запускати

| Задача | Модель | Reasoning | Чому саме вона |
|---|---|---|---|
| **T1** | **Sol** | `xhigh` | Проєктування shared API, від якого залежать усі три поверхні; помилка в контракті помножиться на весь подальший рефакторинг. |
| **T2** | **Sol** | `xhigh` | Зміна центрального контракту `PlatformButtons`/`CommentInjector` зі станом, кешами і lifecycle прив'язки подій. |
| **T3** | **Terra** | `high` | Локальна реалізація в одному файлі за вже визначеним контрактом. |
| **T4** | **Terra** | `high` | Те саме для Studio; додатково badge/dropdown у метаданих. |
| **T5** | **Terra** | `high` | Те саме для StreamYard-панелі (без міграції конвеєра — це T7). |
| **T6** | **Terra** | `medium` | Механічна заміна 28 інлайнових union'ів, але з перевіркою, що семантика в кожному місці однакова. |
| **T7** | **Sol** | `xhigh` | Переведення StreamYard на Adapter Pattern: другий конвеєр із власним станом, ПКМ/коліщатко, auto-heal. Найризикованіша задача набору. |
| **T8** | **Sol** | `high` | Дескриптори полів аркуша зачіпають storage-ключі, легасі-фолбек і відновлення стану — тут легко тихо втратити дані користувача. |
| **T9** | **Terra** | `high` | Обмежена таблиця опцій із однозначною перевіркою через тести options. |
| **T10** | **Sol** | `high` | Спочатку продуктове рішення щодо i18n; без вибору користувача write-частину не починати. |
| **T11** | **Terra** | `medium` | Опис уже наявної архітектури; виконувати ПІСЛЯ T1–T6. |
| **T12** | **Terra** | `medium` | Інструкція «як додати X»; має описувати новий реєстр, не старий стан. |
| **T13** | **Terra** | `medium` | Реструктуризація AGENTS.md без втрати жодного накопиченого правила. |
| **T14** | **Luna** | `medium` | Механічне доповнення трьох скілів розділом «реєстри». |
| **T15** | **Luna** | `medium` | Один рядок у `package.json` + згадка в доках. |
| **T16** | **Luna** | `medium` | Механічне прибирання `docs/`, без видалення історії. |
| **T17** | **Sol** | `xhigh` | Типізація схеми сховища — наскрізна зміна з міграціями і легасі-ключами. |
| **T18** | **Terra** | `high` | Вмикання `noUncheckedIndexedAccess` після T17; механічно, але з великим числом правок. |
| **T19** | **Terra** | `high` | Тести entry points: ланцюжки ініціалізації і моки chrome API. |
| **T20** | **Terra** | `high` | Розкладання `modules/` по шарах — масові правки імпортів, перевіряється build'ом. |

### Порядок і залежності

> **Ланцюг ядра:** **T1 → T2 → (T3 ‖ T4 ‖ T5) → T6 → T7.** T3/T4/T5 можна вести паралельно — вони не перетинаються по файлах. T7 запускати лише коли T1–T6 зелені.
> **Документація після коду:** **T11 і T12 писати ПІСЛЯ T6**, інакше вони зафіксують архітектуру, якої вже не буде.
> **Незалежні:** T8, T9, T14, T15, T16 — можна брати будь-коли.
> **T10 заблокована** рішенням користувача (див. задачу).
> Максимум 3 паралельні агенти з неперетинним file ownership.

---

## P1 — усунення першопричини: єдиний реєстр дій над коментарем

| # | Задача | Файл | Що зробити | Як перевірити |
|---|--------|------|-----------|---------------|
| **T1** | Набір дій над коментарем ніде не описаний одним місцем — три незалежні описи на трьох поверхнях | новий `modules/comment_actions.ts` | Створити реєстр: `{ id, stateType, icon, title, platforms, classNames? }` для `copy` / `question` / `prayer`. Експортувати `CommentActionId`, `ButtonStateType` (виводиться з реєстру, а не пишеться руками). Реєстр — чиста таблиця без DOM. | Новий `tests/comment_actions.test.js`: реєстр містить 3 дії, id унікальні, `ButtonStateType` збігається з наявним `'question' \| 'prayer' \| null`. `npm run typecheck` 0. |
| **T2** | `PlatformButtons` — фіксовані 3 іменовані слоти; `CommentInjector` має окремий метод на кожну дію | `modules/comment_platform_adapter.ts:18-24`, `modules/comment_injector.ts:87-147` | `PlatformButtons` → `{ buttons: Partial<Record<CommentActionId, HTMLElement \| null>>, checkboxEl, bodyEl }`. У `CommentInjector` замість `handleQuestionClick`/`handlePrayerClick`/`handleCopyClick` — один диспетчер за `id` дії з реєстру. Зберегти поточну поведінку 1-в-1 (включно з ПКМ/коліщатком). | `npm test` (зокрема `tests/comment_injector*.test.js`, `tests/yt_ui_api.test.js`, `tests/studio_*.test.js`) — 0 fail. Ручна перевірка: кнопки на YouTube і Studio працюють як раніше. |
| **T3** | Панель YouTube описує кнопки власною таблицею `PANEL_BUTTONS` | `youtube/yt_comment_panel.ts:23-42` | Будувати панель із реєстру `comment_actions`, лишивши в файлі лише платформні оверайди (класи `syh-yt-btn-*`, текстові підписи). Локальну таблицю видалити. | `tests/yt_ui_api.test.js` зелений; кількість і порядок кнопок у DOM не змінились. |
| **T4** | Панель Studio описує ті самі 3 кнопки третім набором імен (`studio-*`) | `youtube/studio/studio_ui.ts:32-52` | Те саме з реєстру; імена дій уніфікувати з реєстром, префікс класів `syh-studio-btn-*` лишити як платформний оверайд. Перевірити, що `applyButtonState` у `studio_adapter.ts` читає новий контракт. | `tests/studio_ui*.test.js`, `tests/studio_binding*.test.js` зелені. |
| **T5** | Панель StreamYard описує ті самі дії четвертим набором імен (`copy-author-comment`) | `modules/ui_comments.ts:23-38` | Будувати панель із реєстру. Історичні `data-action` лишити як alias-мапу в реєстрі, щоб не зламати `event_comments/button_handlers.ts` і `handlers/context_menu.ts` до виконання T7. | `tests/ui_comments*.test.js`, `tests/event_comments*.test.js` зелені; ЛКМ/коліщатко/ПКМ на 🙏 дають ті самі три різні результати. |
| **T6** | Літерал `'question' \| 'prayer'` вписаний 28 разів у 16 файлах | 16 файлів (перелік — у розділі «Кількісне підтвердження» аудиту) | Замінити інлайнові union'и на імпорт типу з реєстру. Перед заміною переконатись, що в кожному місці семантика та сама (а не, скажімо, лише підмножина для збережених елементів). | `npm run typecheck` 0, `npm test` 0 fail, `grep -c "'question' | 'prayer'"` → лише реєстр. |
| **T7** | StreamYard не переведено на Adapter Pattern — паралельний конвеєр на 19 файлів | `modules/event_comments/*` → новий `modules/streamyard_adapter.ts` | Реалізувати `CommentPlatformAdapter` для StreamYard і перевести обробку на `CommentInjector`. Згорнути `event_comments/*`, зберігши особливості: ПКМ/коліщатко для 🙏, `data-syh-just-added`, auto-heal. Виконувати окремою хвилею, з поетапними комітами. | Повний `npm test` + ручна перевірка StreamYard за `docs/manual testing/`. Порівняти поведінку до/після по чеклісту з 3 типів кліку. |

---

## P2 — та сама хвороба в полях і опціях

| # | Задача | Файл | Що зробити | Як перевірити |
|---|--------|------|-----------|---------------|
| **T8** | Поле аркуша попапу вимагає ручної реєстрації в 6 місцях | `popup/popup.html` (template), `popup/popup_sheet_renderer.ts:20-45`, `modules/storage_keys.ts`, `popup/popup_sheet_keys.ts`, `popup/popup_sheet_field_restorer.ts`, `popup/popup_sheet_bindings.ts` | Ввести таблицю дескрипторів поля: `{ cssHook, idPrefix, storageKey, legacyPrefix, kind: 'text' \| 'textarea' \| 'html' }`. Рендер (`setAttrId` × 31), відновлення і збереження — цикли по таблиці. Легасі-фолбек `tg_*__` зберегти. | `npm test` (`tests/popup_sheet*.test.js`) 0 fail. Окремо перевірити відновлення старих даних: заповнити аркуш на попередній версії → оновити розширення → значення на місці. |
| **T9** | Нова опція вимагає правок у 5–7 паралельних списках | `options/defaults.ts`, `options/form.ts:28-60`, `options/validation.ts`, `options/options.html` | Один масив дескрипторів опцій (`id`, `elementId`, `kind`, `default`, `validate`). `populateFormElements` / `readOptionsFromForm` / `validation` — цикли по ньому. `OptionsState` виводиться з масиву. | `tests/options_settings.test.js`, `tests/options_controller_io.test.js` зелені; експорт/імпорт конфігурації дає той самий JSON, що й до зміни. |
| **T10** | i18n не масштабується: 54 ключі перекладу проти сотень літералів у коді | `_locales/*/messages.json`, UI-модулі | **Спершу рішення користувача:** (а) визнати проєкт україномовним і прибрати недопрацьований i18n-шар; (б) ввести правило «нова кнопка = ключ у `messages.json`» і поступово винести літерали. Реалізацію не починати без вибору. | Після вибору: якщо (а) — `default_locale` і залишки узгоджені, тести зелені; якщо (б) — нові рядки з'являються лише через `chrome.i18n.getMessage`. |

---

## P3 — документація і вказівки агентам

| # | Задача | Файл | Що зробити | Як перевірити |
|---|--------|------|-----------|---------------|
| **T11** | Карта архітектури живе лише всередині аудиту і помре разом із ним | новий `docs/ARCHITECTURE.md` | Перенести й актуалізувати карту шарів (Presentation / Orchestration / Domain / Infrastructure) + **список усіх реєстрів проєкту** з посиланнями на файли. Писати після T6. | Кожен шлях у документі існує (перевірити скриптом/грепом); документ згаданий у `README.md` і `AGENTS.md`. |
| **T12** | Найчастіша операція («додати кнопку/поле/опцію») ніде не описана | новий `docs/HOWTO_ADD.md` | 4 сценарії: додати дію над коментарем · поле аркуша · опцію · нову платформу. Кожен — список кроків із конкретними файлами і командою перевірки. Після T1–T9 кожен сценарій має скоротитись до 1–2 кроків. | Пройти сценарій «додати дію» руками на тестовій гілці — кроки збігаються з реальністю. |
| **T13** | `AGENTS.md` = 101 рядок правил суцільним списком, що росте по рядку за коміт | `AGENTS.md` → `docs/rules/*.md` | Лишити в `AGENTS.md` короткий протокол (комунікація, аудит, команди, SSOT-правило як **інструкція до дії**: «знайди реєстр → якщо немає, створи реєстр, а не другу копію»). Тематичні правила винести в `docs/rules/` (парсери, транслітерація, happy-dom, storage, studio DOM). Жодне правило не загубити. | Порівняти список правил до/після (кількість пунктів збігається); `AGENTS.md` ≤ 60 рядків. |
| **T14** | Скіли не називають реєстри, тому агент не знає, куди дописувати | `.agents/skills/*.md` | Додати в кожен скіл розділ «Реєстри цієї поверхні» з посиланнями на `comment_actions.ts`, `modules/sheets.ts`, `STORAGE_KEYS`, `POPUP_SHEET_KEYS`, `UiFactory`, `config.ts` (селектори). Правило: селектор у скілі — це посилання на `config.ts`, а не скопійований рядок. | Грепом переконатись, що в скілах немає захардкоджених селекторів поза посиланнями. |
| **T15** | Чотири перевірки запускаються окремо, тому легко пропустити одну | `package.json` | Додати `"verify": "npm run typecheck && npm run lint && npm test && npm run build"`. Згадати в `AGENTS.md`, `README.md`, `docs/HOWTO_ADD.md`. | `npm run verify` проходить із нуля на чистому клоні. |
| **T16** | `docs/` розрісся до 123 файлів, актуальний зріз важко знайти | `docs/` | Розвести актуальне/архів; злити `DEVELOPER_NOTES.md` + `walkthrough.md` + `Daily_tips.md` у один довідник; додати `docs/README.md` з навігацією. Історію аудитів не видаляти. | У `docs/` корені лише навігація + актуальні документи; усі посилання в `README.md`/`AGENTS.md` живі. |

---

## P4 — технічний борг (залишки аудиту 2026-09-05)

| # | Задача | Файл | Що зробити | Як перевірити |
|---|--------|------|-----------|---------------|
| **T17** | 99 `Record<string, any>` у роботі зі сховищем — жодної перевірки типів там, де живуть дані користувача | `modules/storage*.ts`, `modules/sheet_repository.ts`, споживачі | Описати схему сховища типами (ключ → форма значення), звузити API. Робити поетапно, по групах ключів, кожен крок — окремий коміт. | `npm run typecheck` 0 після кожного кроку; `npm test` 0 fail; кількість `Record<string, any>` зменшується монотонно. |
| **T18** | `noUncheckedIndexedAccess` вимкнено — індексний доступ до `Record` мовчки вважається безпечним | `tsconfig.json` | Увімкнути прапорець після T17 і полагодити наслідки. | `npm run typecheck` 0, `npm test` 0 fail. |
| **T19** | Entry points без тестів: `main.ts`, `youtube_content.ts`, `studio_init.ts`, `popup_listeners.ts` | `tests/` | Додати тести ланцюжка ініціалізації: порядок викликів, поведінка при винятку в одному з модулів (перевірити, що error boundaries з `70c0093` реально тримають). | Нові тести падають, якщо прибрати try/catch у `bootstrap_app.ts` (перевірити мутацією вручну). |
| **T20** | 123 файли пласко в `modules/` | `modules/` | Розкласти по шарах (`domain/`, `infra/`, `ui/`, `platform/`) відповідно до карти з T11. Робити одним махом, окремим комітом, без правок логіки. | `npm run verify` зелений; діф містить лише переміщення і зміни шляхів імпорту. |

---

## Що вже зроблено 2026-09-08 (до цього списку)

| Дія | Коміт |
|---|---|
| Сміття з кореня (`split.cjs`, `tmp_banner_modal.json`) → `scratch/`, ігнорується | `e182c08` |
| 60+ файлів рефакторингу хвиль 1–4 розкладено на коміти | `70c0093`, `a7d41a4` |
| Прямі тести `banner_modal_draft` (8) і `banner_modal_parser` (13); 2077 → 2098 | `c1c7450` |
| Звіт аудиту | `def2c69` |
