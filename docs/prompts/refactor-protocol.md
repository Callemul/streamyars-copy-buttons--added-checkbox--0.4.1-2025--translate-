Етап 3. Використай MCP-інструменти Fallow `check_health` та `dead_code`. Якщо MCP не працює,
виконай термінальні команди:
  npx fallow health --max-crap 30 --format json
  npx fallow dead-code --circular-deps --re-export-cycles --unresolved-imports --format json

⚠️ КРОК 0. ПЕРЕДПОЛЬОТНА ПЕРЕВІРКА ТА АУДИТИ:
1. Перевір вміст папки `active/` та прочитай наявні звіти `audit_*.md`. НЕ виправляй описані
   там старі баги — це не твоя зона відповідальності на зараз.
2. Виріши критичні блокуючі помилки структури, які вже є в поточному звіті:
   - Якщо у звіті є "Unresolved imports" або "Re-Export Cycles" у файлах, які ми збираємося
     рефакторити — виправ їх у першу чергу.

🛑 ЕТАП 0.5 — АВТОСТОП (детектор "пустої колотушки"):
ПЕРЕД тим як переходити до АЛГОРИТМУ РЕФАКТОРИНГУ, збери ці 5 сигналів і перевір умову зупинки.
Якщо умова виконується — НЕ роби жодних правок, не вигадуй роботу, не чіпай тести/сетап/косметику.
Просто повідом: "Проєкт уже чистий (health-score __, tests зелені, 0 структурних боргів) —
чистити нічого. Зупинка за автостопом." і заверши сесію.

Сигнали:
  a) `npx fallow dead-code --circular-deps --re-export-cycles --unresolved-imports --format json`
     → circular_dependencies == 0, re_export_cycles == 0, unresolved_imports == 0.
  b) `npx fallow health --max-crap 30 --format json` → відфільтруй знахідки, ВИКЛЮЧАЮЧИ
     файли тестів і сетапу (`*.test.js`, `*.test.ts`, `tests/setup/**`, `tests/**`, `ts_loader.js`).
     Залишились продакшн-хотспоти з CRAP > 30 (або cyclomatic > 20 / cognitive > 15)?
  c) `npm run test` → fail == 0.
  d) `npx tsc --noEmit` → 0 помилок типізації.
  e) `npm run lint` → 0 errors І 0 warnings про unused-imports/exports/types У ПРОДАКШЕН-коді
     (попередження лише у тестах ігноруємо).

УМОВА ЗУПИНКИ (усі пункти разом):
  a) виконується І b) продакшн-хотспотів НЕМАЄ І c) виконується І d) виконується І e) виконується.

Якщо умова виконується → СТОП. НЕ вважай "пустою колотушкою" наявність:
  - документованих багів у `active/` (їх фіксують за окремим правилом, див. нижче);
  - штрафів `unit_size` / `coupling` за fallow, якщо вони походять від тестів/сетапу;
  - попереджень лінтера лише у тестових файлах.
Це — косметика, не привід ламати зелений стан.

🤖 АЛГОРИТМ РЕФАКТОРИНГУ (виконується ТІЛЬКИ якщо автостоп на ЕТАПІ 0.5 НЕ спрацював):
1. Знайди 2-3 модулі із найвищим ризиком (CRAP > 30) або зацикленими залежностями
   (пріоритет: modules/messaging.ts, modules/ui.ts — якщо вони ще не розбиті).
2. Напиши характерні Unit-тести для їхніх публічних експортованих API.
3. Розбий ці модулі на менші файли та розплутай зациклені імпорти.
4. Після кожної зміни запускай тести, щоб переконатися, що логіка під час рефакторингу
   збережена 1-в-1.
5. **ФІНАЛЬНА ФАЗА:** Якщо під час роботи над цими модулями ти знайшов нові баги — виправ їх зараз.

⚠️ КРИТЕРІЇ УСПІШНОГО ЗАВЕРШЕННЯ (DEFINITION OF DONE):
1. СТОП-УМОВА: 100% ЗЕЛЕНІ ТЕСТИ — усі тести до єдиного проходять. Якщо хоча б один
   падає через твої зміни — адаптуй код/тести; не лишай репозиторій із червоними тестами.
2. СТОП-УМОВА: ПРИБИРАННЯ ЗА СОБОЮ — `npx fallow dead-code --format json` має показати
   0 new unused-imports/exports/types У ЗМІНЕНИХ ТОБОЮ файлах. Якщо переніс функцію, а
   старий імпорт лишився — видали.
3. СТОП-УМОВА: УСПІШНИЙ TYPE-CHECK — `npx tsc --noEmit` без жодної помилки.

⚠️ ПРАВИЛО РОБОТИ З НОВИМИ БАГАМИ (ВІДКЛАДЕНЕ ВИПРАВЛЕННЯ):
1. НЕ виправляй їх миттєво — рефакторинг зберігає поведінку 1-в-1.
2. Виправ після завершення базового рефакторингу, коли всі тести зелені.
3. Після виправлення онови/додай Unit-тести на нову логіку.
4. ВИНЯТОК: якщо виправлення вимагає глобальних змін архітектури — створи звіт у `active/`
   за шаблоном нижче (НЕ виправляй у цій сесії).

СТРУКТУРА ФАЙЛУ В `active/` (лише якщо баг неможливо/небезпечно виправити зараз):
---
# [2026-ММ-ДД] — [НАЗВА_МОДЕЛІ] — Аудит: латентний баг [Контекст]
> **Контекст виявлення:** знайдено під час рефакторингу CRAP-хотспотів.
> **Статус:** ⛔ НЕ ВИПРАВЛЕНО (занадто великий scope). Рефакторинг зберіг поведінку 1-в-1.
---
## 1. Що саме зламано
## 2. Runtime-наслідки
## 3. Пропоноване виправлення
## 4. Де зараз живе цей борг у коді
