# Чекліст ручної перевірки — нічна хвиля 2026-09-08/09

> **Навіщо:** автотести йдуть на `happy-dom` і не бачать реального StreamYard,
> реального `chrome.storage` і реального рендеру попапа. Усе, що нижче, —
> перевіряється **в браузері**, одним заходом.
>
> **Що змінилось у цій хвилі:** T7 (StreamYard переведено на
> `CommentPlatformAdapter`), запобіжник до T7 (інваріант панель+прив'язка),
> T8 (дескриптори полів аркуша попапу), T9 (дескриптори опцій), T19 (тести
> entry points).
>
> **Як користуватись:** `npm run build` → `chrome://extensions` → «Завантажити
> розпаковане» → папка `dist/`. Кожен пункт — що відкрити, що натиснути, що
> має статися.

---

## 1. StreamYard — дії над коментарем (T7, найризикованіше)

Відкрити `https://streamyard.com/…` → студія з живими коментарями (вкладка
«Коментарі»), консоль F12 відкрита — стежити, чи немає помилок.

### 1.1. Кнопка 🙏 — три різні результати від трьох кнопок миші

Це головна відмінність StreamYard від YouTube: результат залежить від того,
**якою кнопкою миші** натиснули. Після T7 це описано в реєстрі (`mouseButtons`),
а не зашито в обробнику — тобто саме тут найлегше було щось загубити.

- [ ] **ЛКМ** по 🙏 → у буфер лягає варіант `🙏🙏🙏`; з'являється банер копіювання
- [ ] **Коліщатко (середня кнопка)** по 🙏 → у буфер лягає варіант `🙏❤️🙏`
- [ ] Коліщатко **не** відкриває нову вкладку і не вмикає автоскрол сторінки
- [ ] **ПКМ** по 🙏 → у буфер лягає варіант `❤️❤️❤️`
- [ ] ПКМ по 🙏 **не** відкриває контекстне меню браузера
- [ ] Після кожного з трьох кліків запис з'являється в базі молитов (попап →
      вкладка «Молитовні»), і саме один раз, не тричі

### 1.2. Решта кнопок панелі

- [ ] **📄** (копіювати коментар) → у буфері текст коментаря, кнопка коротко «спалахує»
- [ ] **❓** (копіювати автора + коментар) → у буфері автор і текст, запис
      потрапляє у список питань
- [ ] Порядок кнопок у панелі: 📄 · ❓ · 🙏 · чекбокс — зліва направо, без пропусків
- [ ] Іконки й підказки (`title` при наведенні) ті самі, що були до хвилі

### 1.3. Чекбокс «опрацьовано»

- [ ] Клік по чекбоксу відмічає коментар
- [ ] **ПКМ по тілу коментаря** також перемикає чекбокс (це історична поведінка)
- [ ] Стан чекбокса тримається **за текстом коментаря**: якщо StreamYard
      перемалював список, галочка лишилась на тому самому коментарі
- [ ] Стан чекбокса **не** переживає перезавантаження сторінки (він в оперативному
      `SYH_STATE`, а не в `chrome.storage`) — це очікувано, не баг

### 1.4. Auto-Heal і зірка платформи

- [ ] Натиснути ⭐ на коментарі → зняти ⭐ → панель наших кнопок повернулась
      на місце й **кнопки працюють** (натиснути 📄 і переконатись, що копіює)
- [ ] Перемкнутись на вкладку «Starred» і назад → кнопки на місці й робочі
- [ ] Прогорнути список коментарів угору-вниз кілька разів (StreamYard
      перемальовує картки) → у нових карток кнопки з'явились і працюють
- [ ] На картці, яку щойно позначили ❓/🙏, атрибут `data-syh-just-added`
      тримається ~2 с і зникає (перевірити в Elements)

### 1.5. Запобіжник цієї хвилі — панель без прив'язки

Це те, що ловить новий тест `tests/streamyard_panel_binding_invariant.test.js`.
У браузері симптом виглядав би так: **кнопки на місці, натискаєш — нічого**.

- [ ] Пройти пунктами 1.1–1.4 і переконатись, що **немає жодної картки**, де
      кнопки намальовані, але не реагують
- [ ] В Elements у контейнера `.syh-custom-buttons-comment` кожної картки з
      кнопками є атрибут `data-syh-events-bound="true"`

### 1.6. Перезавантаження розширення при вже відкритій сторінці

Класичне джерело `Extension context invalidated`.

- [ ] Відкрити студію з коментарями → `chrome://extensions` → 🔄 перезавантажити
      розширення → повернутись на вкладку студії **без перезавантаження сторінки**
- [ ] Натиснути 📄 на коментарі: або кнопка працює, або в консолі зрозуміле
      повідомлення — але **не** мовчазна тиша і не червона стіна помилок
- [ ] Після F5 сторінки все працює як звичайно

---

<!-- Наступні розділи дописуються після виконання відповідних задач -->

## 2. Попап — аркуші та поля (T8)

Відкрити попап (іконка розширення) → вкладка «Телеграм».

### 2.1. Поля зберігаються і відновлюються

Для КОЖНОГО аркуша (вкладки-канали внизу: ВП СШ, Опарін, Молчанов СШ,
Молчанов проповідь) — принаймні для двох різних:

- [ ] Заповнити «1. Старий список» → закрити попап → відкрити знову: текст на місці
- [ ] Заповнити «2. Номери ВІДПОВІДЕЙ» → закрити/відкрити: значення на місці
- [ ] Заповнити «3. Новий список» (ліва колонка, Telegram) → закрити/відкрити: на місці
- [ ] Натиснути «ОБРОБИТИ КОМЕНТАРІ» → у блоці «Результат» з'явився текст →
      закрити/відкрити: результат на місці
- [ ] Відредагувати текст прямо в блоці «Результат» (він contenteditable) →
      клацнути поза ним → закрити/відкрити: правка збереглась

### 2.2. Лічильники й похідний стан

- [ ] Лічильник біля «1. Старий список» перераховується при вводі
- [ ] Лічильники «Разом / Telegram / YouTube» у кроці 3 змінюються при вводі
- [ ] Панель статистики («Залишилось старих / Видалено / Нові / Разом») після
      обробки видима → закрити/відкрити попап: панель і далі видима з тими ж числами
- [ ] Розгорнути «🗑 Лог видаленого» → закрити/відкрити: лог розгорнутий,
      лічильник «(N)» той самий
- [ ] Згорнути «🧹 Лог чистки тексту» → закрити/відкрити: лишився згорнутим
- [ ] Перетягнути роздільник між колонками кроку 3 → закрити/відкрити:
      ширина колонок збереглась

### 2.3. Аркуші не змішуються

- [ ] Ввести РІЗНИЙ текст у «Старий список» на двох різних аркушах
- [ ] Перемкнутись між ними кілька разів — тексти не переплуталися
- [ ] Закрити/відкрити попап — обидва на своїх місцях

### 2.4. СТАРІ ДАНІ (найважливіше в T8)

T8 переписав те, ЯК читаються ключі сховища. Автотест на легасі-ключі є
(`tests/popup_sheet_fields.test.js`), але його треба підтвердити на живих даних.

- [ ] **Перед оновленням розширення** заповнити аркуш на попередній версії
      (старий список, номери відповідей, новий список, результат, розгорнути логи)
- [ ] Оновити розширення до цієї збірки (`chrome://extensions` → 🔄)
- [ ] Відкрити попап: **усі** значення на місці, логи в тому ж стані,
      лічильники ті самі, ширина колонок та сама
- [ ] Якщо щось зникло — **не продовжувати**, зафіксувати, яке саме поле

### 2.5. Очищення

- [ ] Кнопка 🗑 «Очистити» біля «ОБРОБИТИ КОМЕНТАРІ» питає підтвердження
- [ ] Після підтвердження поля аркуша порожні, інші аркуші не постраждали
- [ ] Кнопка 🗑 «Очистити зібрані» в колонці YouTube Studio чистить лише її

## 3. Сторінка налаштувань (T9)

Відкрити: іконка розширення → «Налаштування» (або `chrome://extensions` →
«Параметри розширення»).

### 3.1. Кожна опція жива

Для КОЖНОЇ опції: змінити → «Зберегти» → перезавантажити сторінку налаштувань →
значення на місці.

- [ ] «Назва уроку СШ» (текст)
- [ ] «Назва проповіді» (текст)
- [ ] «Мова інтерфейсу» (випадний список)
- [ ] «Anti-AFK» (чекбокс)
- [ ] «Інтервал Anti-AFK» (число, 5–300, крок 5)
- [ ] «Auto-Heal» (чекбокс)
- [ ] «Довжина обрізання тексту» (число, 50–500)
- [ ] «Компактні вторинні вкладки» (чекбокс)
- [ ] «YouTube» (чекбокс)
- [ ] «YouTube Studio» (чекбокс)

### 3.2. Опції справді впливають на поведінку

Опція без споживача — це те, що вже одного разу довелося видаляти.

- [ ] Вимкнути «YouTube» → відкрити youtube.com з трансляцією: наших кнопок немає
- [ ] Вимкнути «YouTube Studio» → відкрити Studio: панелі немає
- [ ] Змінити «Довжину обрізання» на маленьке число → скопіювати довгий
      коментар: текст обрізано на нову довжину
- [ ] Вимкнути Auto-Heal → зняти ⭐ з коментаря: кнопки НЕ відновлюються самі
- [ ] Змінити назви програм → у попапі підписи вкладок оновились

### 3.3. Фолбеки (тут T9 найлегше було зламати)

- [ ] Стерти «Назву уроку СШ» дочиста → зберегти → перезавантажити:
      підставилось «СШ Урок», а не порожньо
- [ ] Стерти «Інтервал Anti-AFK» → зберегти: стало 30, а не порожньо і не NaN
- [ ] Ввести в «Інтервал» текст `abc` → зберегти: стало 0 (не NaN)
- [ ] **ВИМКНУТИ** чекбокс (напр. Anti-AFK) → зберегти → перезавантажити:
      лишився вимкненим (не «повернувся» в увімкнений за замовчуванням)
- [ ] Поставити «Довжину обрізання» = 0 → зберегти → перезавантажити:
      лишився 0, не підмінився на 195

### 3.4. Експорт / імпорт конфігурації (критерій приймання T9)

- [ ] Налаштувати все під себе → «Експортувати» → зберігся файл
      `streamyard_helper_config_РРРР-ММ-ДД.json`
- [ ] Відкрити файл у текстовому редакторі: секція `syh_options` містить усі
      10 опцій з правильними значеннями
- [ ] **Якщо є експорт зі старої версії** — порівняти дві секції `syh_options`:
      склад ключів і їхній порядок мають збігатися
- [ ] Скинути налаштування («Скинути») → «Імпортувати» цей файл → усі значення
      повернулись, сторінка показує їх у формі
- [ ] Імпортувати навмисно битий JSON → повідомлення про помилку, налаштування
      не зіпсовані

### 3.5. Скидання

- [ ] «Скинути» питає підтвердження
- [ ] Після скидання всі поля мають значення за замовчуванням
      (СШ Урок / Проповідь / auto / Anti-AFK увімк. / 30 / Auto-Heal увімк. /
      195 / компактні вкладки увімк. / YouTube увімк. / Studio увімк.)

## 4. Стійкість до збоїв (T19)

T19 додав автотести на error boundaries — перевірку, що падіння одного модуля
не вбиває решту розширення. Автотести підміняють модулі; у браузері те саме
перевіряється грубішим способом.

- [ ] Відкрити StreamYard, у консолі (F12) не має бути червоних помилок при старті
- [ ] У консолі має бути рядок `StreamYard Helper vX.X.X … Loaded!` і `SYH is running.`
- [ ] Відкрити ту саму студію у ДВОХ вкладках — обидві працюють незалежно
- [ ] У консолі однієї вкладки не має бути попередження про повторну
      ініціалізацію (воно очікується лише якщо скрипт вставився двічі в одну сторінку)
- [ ] YouTube: перейти між кількома відео БЕЗ перезавантаження сторінки
      (SPA-навігація) → кнопки з'являються на кожному новому відео
- [ ] YouTube: перейти на сторінку каналу поза списком дозволених → наші кнопки
      прибрались, помилок у консолі немає
- [ ] Studio: відкрити вкладку коментарів → панель є; перемкнути відео → панель є
- [ ] Попап: відкрити після повного очищення сховища
      (`chrome://extensions` → видалити й поставити розширення заново) →
      вікно відкривається, поля порожні, помилок немає

## 5. Після закриття схеми сховища (T17, крок 2)

Зміна суто типова — у рантаймі нічого не мало змінитись. Але один ключ
(`syh:streamyard:expanded_tabs`, стан згорнутих/розгорнутих правих вкладок)
переїхав із літерала в реєстр ключів. Ім'я збережено, тест це фіксує, але
підтвердити на живих даних варто.

- [ ] StreamYard: згорнути кілька правих вкладок → перезавантажити сторінку →
      вкладки лишились згорнутими
- [ ] Розгорнути назад → перезавантажити → лишились розгорнутими
- [ ] Увімкнути/вимкнути «Компактні вторинні вкладки» в налаштуваннях →
      поведінка правих вкладок змінилась відповідно
- [ ] Загальна перевірка, що нічого не осиротіло: пройти розділ 2.4
      (старі дані попапу) ще раз після цієї збірки

## 6. Після строгої перевірки індексів (T18)

Прапорець `noUncheckedIndexedAccess` змусив переписати 76 місць. Зміни
навмисно поведінково-нейтральні, і це підтверджено 2202 автотестами плюс
окремою звіркою переписаного `levenshtein` з еталонною реалізацією
(4006 пар рядків, 0 розбіжностей). Але переписані ділянки варто пройти
руками — вони зачіпають розбір тексту, а там помилка тиха.

### 6.1. Транслітерація і пошук (`utils_text`, `fuzzy_match`)

- [ ] Попап → вкладка транслітерації: ввести український текст у ліве поле →
      праве заповнюється правильно, діакритика й диграфи (ї, є, щ, ьо) на місці
- [ ] Ввести текст із помилковою розкладкою (`ghbdsn`) → перемикання розкладки
      дає осмислене слово
- [ ] Пошук по коментарях: знайти коментар із ОДНІЄЮ помилкою в слові
      (нечіткий пошук) → коментар знаходиться
- [ ] Пошук по імені автора в іншій розкладці/транслітерації → знаходиться

### 6.2. Розбір Телеграм-списку (`telegram_old_*`, `emoji_parser`)

- [ ] Вставити старий список із авторами, часом («2 години тому») і підпунктами →
      «ОБРОБИТИ» → структура збережена, автори не злиплись
- [ ] Блок із ОДНИМ підпунктом → у результаті він без маркера підпункту
- [ ] Блок із кількома підпунктами → нумерація 1) 2) 3) на місці
- [ ] Emoji-нумерація (1️⃣ 2️⃣ … 🔟, 1️⃣2️⃣) рендериться правильно, зокрема для
      номерів 10 і більше
- [ ] Лог чистки тексту показує ті самі записи, що й раніше

### 6.3. Банери і завантаження (`banner_modal_parser`, `video_copier`)

- [ ] Створення банерів із модального вікна: кілька повідомлень поспіль →
      усі банери створені, жоден не порожній
- [ ] Розділення на блок питань і блок молитов працює
- [ ] Послідовне завантаження відео-карток: усі файли завантажились по черзі

### 6.4. Статистика (`stats_math`, `stats_header_controls`, слайди)

- [ ] Кнопки «❓ Старт: Питання» і «🙏 Старт: Молитви» є в шапці, обидві,
      у правильному порядку і працюють
- [ ] Стан кнопок відновлюється після перезавантаження сторінки
- [ ] Модальне вікно аналітики: медіана глядачів рахується правильно
      (перевірити на парній і непарній кількості замірів)
- [ ] Експорт слайда в PNG працює

### 6.5. Попап і Studio

- [ ] Розміри textarea (перетягнути кут) зберігаються й відновлюються
- [ ] Імпорт конфігурації: обрати файл → імпорт; скасувати вибір файлу →
      нічого не ламається
- [ ] Studio: випадний список аркушів показує підписи, а не «undefined»
- [ ] Studio: бейдж категорії на кнопках ❓/🙏 показує назву аркуша
